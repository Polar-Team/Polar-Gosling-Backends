name: compose-smoke
# TODO: implement in task 12.1 of the docker-compose-cloud-stack-testing spec.
# Replace this placeholder with the full workflow that runs `make compose-up`,
# `make compose-smoke`, and `make compose-down` against the Cloud_Stack.
